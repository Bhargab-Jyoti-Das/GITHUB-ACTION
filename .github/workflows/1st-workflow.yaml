name: 01 - workflow

on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps: 
          - name: Say hello
            run: echo "Hello Bhadas"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
          - name: Failed step
            run: |
               echo "I Will Fail"
               exit 1
          - name: say goodbye
            run: echo "goodbye"
     